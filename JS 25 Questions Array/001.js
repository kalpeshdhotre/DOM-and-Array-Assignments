// 1. Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with there type.

var varName = "kalpesh";
console.log(varName + " is of type " + typeof varName);

var isOddnumber = true;
console.log(isOddnumber + " is of type " + typeof isOddnumber);

var notDefined;
console.log(notDefined + " is of type " + typeof notDefined);

var noValue = null;
console.log(noValue + " is of type " + typeof noValue);
