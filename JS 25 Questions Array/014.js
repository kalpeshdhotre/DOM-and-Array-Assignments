// 14. Slope is m = (y2-y1)/(x2-x1). Find the slope between point (2, 2) and point(6,10)

let point1 = [2, 2];
let point2 = [6, 10];

let slope = (point2[1] - point1[1]) / (point2[0] - point1[0]);

console.log(`Slope for the given co-ordinates ${point1} and ${point2} is : ${slope}`);
